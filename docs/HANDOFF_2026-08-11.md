# Deckd handoff — 2026-08-11

Session died mid-verification (iteration limit + transient processing error) while confirming the solo blackjack flow. This doc is the single source of truth for any fresh session: what exists, what's proven, what's next. Read `STATUS.md` and `docs/LOOP_DIRECTIVES.md` alongside this.

## Where we are

Deckd is functionally launch-ready on web. All four pillars from the Sol audit (2026-08-10) are DONE and committed on `cleanup/ready-to-build`:

1. **Solo blackjack** — `cc06d8e` (+ 2 engine tests, 87 total)
2. **Offline shell (PWA)** — `c017844` (service worker, immutable hashed assets)
3. **Multiplayer hardening** — `3b38df2` + `f7dd9b4` (reconnect with seat/room grace, recipient-filtered event broadcast, no hidden-card leaks, second-game sync cursor, honest store)
4. **Monetisation honesty** — fake banner gone, no free cosmetic unlocks, IAP fails closed (still needs Josh: RevenueCat products/keys)

Also landed: rules engine + court cards + nav sweep (`8f47622`), poker/blackjack fixes (`571daa6`, `7970086`), presets page redesign (`4d6a51a`).

## Last session (2026-08-10 evening, died early 2026-08-11)

The session was verifying the solo blackjack loop end-to-end in the browser when it hit the tool-iteration cap and then a processing error ("no response generated"). The verification was NOT completed in that session, but:

- The full solo loop had ALREADY been proven live earlier (deal → TWIST → bust → house auto-play → HAND OVER → Next hand → fresh deal), recorded in `references/solo-offline-batch-2026-08-10.md` and the skill.
- The offline cold-launch proof (server killed, app boots from cache, game state intact) was also already done and recorded.
- Working tree is CLEAN — nothing uncommitted, nothing lost.

## Proven (don't redo)

- 87/87 jest tests green (last verified pass), typecheck, lint, expo-doctor 20/20.
- Solo blackjack full loop, live browser proof.
- Offline cold launch, live browser proof.
- Public smoke: `DECKD_QA_URL=https://deckd-app.roxai.click node qa/deckd-visual-qa.cjs`.
- Public two-client relay: `qa/deckd-lobby-two-client.cjs` (room created/joined, host draw propagated).
- Public online blackjack E2E: `qa/deckd-lobby-blackjack-qa.cjs` (guest TWIST via `game_action`, host saw the card).
- Reduced-motion web proxy: `qa/deckd-responsive-reduced-motion-qa.cjs`.
- Live deploys: deckd-app.roxai.click (PM2 `deckd-app`, 8085), relay.roxai.click (PM2 `deckd-relay`, 8083), deckd.roxai.click landing (PM2 `deckd-landing`, 8084). All online as of this handoff.

## What's left

**Needs Josh (the only genuinely external items):**
1. RevenueCat/App Store/Play Console products + keys per `docs/MONETIZATION.md` (~1 hour). Sol recommends collapsing 4 SKUs to ONE lifetime Master (~£14.99); Deal/Draw/Shuffle can't grant anything with the current single-boolean entitlement.
2. App Store submission (his Apple account).
3. Physical-device touch/safe-area/reduced-motion pass (no ADB device/emulator on this host; iPhone not remotely automatable).

**Agent-side, in priority order:**
1. Wire `Purchases.addCustomerInfoUpdateListener` → `setMasterPass` (entitlement listener).
2. Live two-client run against the deployed relay AFTER the hardening batch is the one QA gap (unit/E2E proven, not live-proven post-hardening — the pre-hardening live two-client run passed, so this is belt-and-braces).
3. Josh's UI pass whenever he drops it. Keep correctness/infra ahead of design drops.

**Parked (do not start without Josh):** Rive animations, broader game library, recipe DSL, suits/credits, BLE.

## Machine state

- Repo: `C:\Users\Wrekin\Documents\workspace\deckd`, branch `cleanup/ready-to-build`, remote `git@github.com:JoshOsborne1/deckd.git`. Working tree clean at `c017844`.
- Kanban: loop watchdog cron `371dbfff1427` active (every 30m). LOOP card `t_2d6f4e02` is in `todo` (finished work is committed; watchdog will re-dispatch it next tick if nothing else runs). Two follow-up cards queued: `t_37ab1e8a` (native touch/safe-area QA), `t_d590dbf3` (real two-device lobby validation).
- PM2: deckd-app / deckd-relay / deckd-landing all online.
- Deploy gotcha: `npx expo export --platform web` takes 4.5+ min; run as background with notify_on_complete, then verify the new `entry-<hash>.js` landed in `app-serve/_expo/static/js/web/` before calling it done.

## Fresh-session starter

1. `cd /c/Users/Wrekin/Documents/workspace/deckd && git status --short && git log --oneline -5` — expect clean tree at `c017844`.
2. If asked to verify solo: run `npx jest --runInBand` (87 tests) + the live QA scripts above. Browser automation against the live origin needs localStorage cleared first (stale sessions ghost the UI).
3. If Josh says "do the next thing": entitlement listener wiring first, then the post-hardening live two-client E2E.

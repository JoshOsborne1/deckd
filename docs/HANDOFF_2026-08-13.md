# Deckd handoff — loop shutdown (2026-08-13)

Snapshot of the deckd project at the moment the autonomous loop was killed
(Josh, 2026-08-13). This is the truth for anyone (human or agent) picking the
project back up.

## What just happened

- All 5 deckd crons removed: `deckd-loop-watchdog`, `deckd-lane-watchdog`,
  `deckd-builder-watchdog`, `deckd-merge-deploy`, `deckd-skill-index`.
- No live workers at shutdown (verified: zero `hermes chat` / kanban worker
  processes running).
- Blocked card `t_80e9d691` (Golf copy fix — A/K wrap contradiction) archived;
  its worktree + branch removed. Its actual work (Golf engine/UI, commit
  61239e7) was already merged and is live — the card was stale-blocked on a
  dead worker pid.
- Backlog statuses corrected to what actually shipped (see below).

## What is LIVE right now

| Surface | URL | PM2 | State |
|---|---|---|---|
| App preview | https://deckd-app.roxai.click | deckd-app (:8085) | online, restarts 41, serves 5ee2bbb |
| Landing | https://deckd.roxai.click | deckd-landing (:8084) | online |
| Relay | https://relay.roxai.click | deckd-relay (:8083) | online |

- Repo: `C:\Users\Wrekin\Documents\workspace\deckd`, branch
  `cleanup/ready-to-build` @ `5ee2bbb` (2026-08-12 23:34) = the deployed head.
- Merge/deploy state file (`deckd-merge-deploy.state`) `_main` matches 5ee2bbb:
  everything through the last deploy is recorded as shipped.
- All 9 lane branches (`deckd-para-*`) are fully merged into
  cleanup/ready-to-build: 0 commits ahead each. They and their worktrees still
  exist on disk but are inert; safe to delete when wanted.
- `git status` shows uncommitted app-serve/ (live export artifacts: new sound
  wav hashes, entry js) — those were left by the last deploy and are
  intentionally not committed (app-serve is deploy output).
- `docs/BACKLOG.md` has an uncommitted edit right now (this shutdown update).

## What the lanes actually shipped (all merged + live)

- **nav3** — chips on the table edge (directive 13), 4fdc3b5 + full 7-surface QA proof bad3dfd.
- **sound** — expo-audio rework, uiStore mute, native replay fix, playback-proof QA.
- **anim-fx / anim-games / anim-games-b / guestresume** — card flight overlay,
  drag foundation, felt-space transitions, per-game passes (poker, blackjack,
  sevens), draw/discard arrivals animated, motion preview bundle.
- **mpe2e** — hold'em live E2E harnesses with privacy assertions (automated
  proof exists; real 2-device proof still owed).
- **solo** — Golf solitaire full game + FreeCell/Pyramid joker fix + recipe routing.
- **uxpass** — confirm dialogs, hand sort toggle, first-run hint, empty states.
- **main line** — War, Go Fish, Old Maid, Crazy Eights, Sevens, rules sheet +
  replay, Hold'em betting, solo Klondike.

## What is NOT done (real backlog, honest statuses)

- **Hold-to-peek** (P0, directive 19) — never started.
- **Undo for freeplay-family games** — never started.
- **Turn indicator polish** — never started.
- **Live 2-device multiplayer proof** — automated harnesses exist, real-device
  proof owed (relay is up).
- **Card fronts: paper, not pixels** (directive 12 P0 — Josh: "STILL NOT A FAN") —
  queued, never started.
- **Table texture** (directive 9) — queued.
- **Home-as-table / drawer screens / surface morph** (G4) — queued.
- **Monetisation** — parked by directive (store products, keys, RevenueCat
  webhook, app store screenshots).

## How to restart (if ever)

1. Recreate the 4 cron jobs (scripts still exist in
   `~/AppData/Local/hermes/scripts/`): `deckd-loop-watchdog.py`,
   `deckd-lane-watchdog.py`, `deckd-builder-watchdog.py`,
   `deckd-merge-deploy.py`, plus `deckd-skill-index.py` if wanted. All no_agent,
   schedules: watchdogs every 5m, merge-deploy every 10m, skill-index weekly.
2. Dispatch pattern (manual): `rox-loop/scripts/dispatch-card.py` with
   `--project deckd --branch wt/deckd-loop` and a body file.
3. Full machinery docs: `docs/LOOP_RUNBOOK.md`, `docs/LOOP-SYSTEM-DESIGN.md`,
   skill `deckd-loop` (has every pitfall learned).
4. Before any restart, decide: the lane/worktree topology is still on disk
   (11 worktrees). Clean with `git worktree remove --force` per tree, then
   `git worktree prune`, or leave them — they are inert.

## Known live quirks

- Kanban board flaps with concurrent sessions: ALWAYS `hermes kanban --board
  default <cmd>`.
- `deckd-app` has 41 pm2 restarts historically (deploy churn), currently
  stable online.
- app-serve/ is deploy output; don't commit it.
- Node tooling: typecheck needs the project script (stack size workaround);
  bare `npx tsc` stack-overflows on Node 24/TS 6.

# Deckd Cursor context

Read first: `AGENTS.md`, `docs/MVP_SCOPE.md`, `docs/ITERATION_WORKFLOW.md`, `STATUS.md`.

Current rule: build pass-and-play v1. Do not expand to BLE, IAP, Rive, auth, analytics, or custom rules unless explicitly asked.

Use `npm run dev:web` for desktop live iteration and `npm run dev` / `npm run dev:phone` for phone testing.
